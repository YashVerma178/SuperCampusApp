version: '3.8'

services:
  super-campus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: super_campus_app
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
